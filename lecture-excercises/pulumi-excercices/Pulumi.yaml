name: pulumi-excercices
description: A minimal Java Pulumi program with Maven builds
runtime: java
config:
  pulumi:tags:
    value:
      pulumi:template: java
packages:
  dns:
    source: pulumi/pulumi/terraform-provider
    version: 1.2.0
    parameters:
      - hashicorp/dns
